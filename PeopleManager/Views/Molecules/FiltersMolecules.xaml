<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PeopleManager.Views.Molecules.FiltersMolecules"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PeopleManager.Views.Molecules"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Filters Fields -->
    <StackPanel Orientation="Vertical" Width="180" Margin="0,0,20,0">
        <TextBlock Text="FILTRAR" Style="{StaticResource SubtitleTextBlockStyle}"/>
        <TextBox x:Name="txtNameFilter" PlaceholderText="Nome" MaxLength="10" Margin="0,5,0,5" />
        <TextBox x:Name="txtSurnameFilter" PlaceholderText="Sobrenome" MaxLength="10" Margin="0,5,0,5" />
        <TextBox x:Name="txtCpfFilter" PlaceholderText="CPF"
                 MaxLength="14"
                 Margin="0,5,0,5" />
                 <!--KeyUp="cpf_KeyUp"-->
        <Button Style="{StaticResource AccentButtonStyle}"
                x:Name="btnFilter" Content="Filtrar"
                Margin="0,5,0,0" HorizontalAlignment="Right" />
                <!--Click="btnFilter_Click"-->
    </StackPanel>
</UserControl>
