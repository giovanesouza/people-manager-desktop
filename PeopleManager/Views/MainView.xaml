<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PeopleManager.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PeopleManager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:organisms="using:PeopleManager.Views.Organisms"
    xmlns:views="using:PeopleManager.Views"
    mc:Ignorable="d"
    Title="People Manager">

    <!--<Grid Margin="10"  Background="#4CD0D0D0">-->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <organisms:HeaderOrganism Grid.Row="0" />
        <views:PeopleView Grid.Row="1" />
    </Grid>
</Window>
