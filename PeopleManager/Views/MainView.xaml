<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PeopleManager.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PeopleManager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:PeopleManager.Views.Organisms"
    mc:Ignorable="d"
    Title="People Manager">

    <Grid Margin="10"  Background="#4CD0D0D0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:HeaderOrganism Grid.Row="0" x:Name="headerApp" />
        <controls:PeopleListOrganism Grid.Row="1" x:Name="listDataGrid" />
    </Grid>
</Window>
